[package]
name = "sphereql"
description = "Project high-dimensional embeddings onto a 3D sphere for semantic search, category enrichment, and visualization"
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true
readme = "../README.md"

[features]
default = ["core"]
core = ["dep:sphereql-core"]
index = ["core", "dep:sphereql-index"]
layout = ["index", "dep:sphereql-layout"]
embed = ["layout", "dep:sphereql-embed"]
graphql = ["index", "dep:sphereql-graphql"]
vectordb = ["embed", "dep:sphereql-vectordb"]
pinecone = ["vectordb", "sphereql-vectordb/pinecone"]
retain-embeddings = ["sphereql-embed/retain-embeddings"]
# Self-contained 3D visualization. Independent of the other features —
# `sphereql-vis` carries its own `sphereql-core` dependency.
vis = ["dep:sphereql-vis"]
full = ["core", "index", "layout", "embed", "graphql", "vectordb", "retain-embeddings", "vis"]

[dependencies]
sphereql-core = { version = "0.3.0", path = "../sphereql-core", optional = true }
sphereql-index = { version = "0.3.0", path = "../sphereql-index", optional = true }
sphereql-layout = { version = "0.3.0", path = "../sphereql-layout", optional = true }
sphereql-embed = { version = "0.3.0", path = "../sphereql-embed", optional = true }
sphereql-graphql = { version = "0.3.0", path = "../sphereql-graphql", optional = true }
sphereql-vectordb = { version = "0.3.0", path = "../sphereql-vectordb", optional = true }
sphereql-vis = { version = "0.3.0", path = "../sphereql-vis", optional = true }
