[package]
name = "sphereql-layout"
description = "Layout engine for sphereQL spatial positioning"
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
keywords = ["spherical", "layout", "clustering", "force-directed", "visualization"]
categories = ["algorithms", "mathematics"]

[dependencies]
sphereql-core = { version = "0.3.0", path = "../sphereql-core" }
sphereql-index = { version = "0.3.0", path = "../sphereql-index" }
rayon = { workspace = true }

[dev-dependencies]
proptest = "1"
approx = "0.5"
rand = "0.8"
